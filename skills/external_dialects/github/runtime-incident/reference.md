# Runtime Incident Reference

This sample mirrors the repository-managed `SKILL.md` style used under `.github/skills/`.

Importer expectations:

1. `tool-allowlist` must normalize to the internal allowlist field.
2. `workflow-template` and `eval-suite` stay as relative asset refs.
3. The sample remains a fixture and does not declare product support for arbitrary GitHub skill dialects.